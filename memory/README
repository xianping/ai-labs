## summary_memory_mannual: 
手动写一个summary，专门一个agent func去做summary

## summary_memroy_langchain:
用langchain去做mannual一样的事情。
 **发现收益不高**。

但，他有几个收益如下：
深度复盘：高级框架在此处的“阳谋”
你仔细观察这段代码，会发现一个非常有意思的现象：哪怕用上了 LangChain 的 LCEL 管道符语法，我们依然在外面包裹了一个 LangChainSummaryMemoryAgent 的 Python 类，并手动写了 if len(...) >= max_buffer_turns: 以及 _compress_memory_internal 触发器。

为什么强大的框架不直接帮我们把这些全部封装成一行代码？

这就是现代高级 AI 架构师必须具备的“清醒认知”：

状态（State）应该归宿于你的系统，而不是模型本身。 真实的生产系统往往伴随着数据库（如 Redis、PostgreSQL）的读写。如果框架用一个黑盒把你所有的记忆逻辑包死，你将无法在记忆压缩的瞬间，将 Summary 同步异步地持久化保存到用户的数据库画像中。

MessagesPlaceholder(variable_name="...") 的降维打击：
在原生 Python 中，我们要小心翼翼地处理 messages.extend（解包列表）和 messages.append（单条添加）的数据结构陷阱。而在 LangChain 中，通过 MessagesPlaceholder，我们可以直接把完整的 [HumanMessage, AIMessage] 数组直接丢进字典，框架会在底层自动完成极其安全、合规的扁平化解包，彻底消灭了人为拼错 API 格式的隐患。

## summary_memroy_langgraph:  
 *这才是langGraph使用的场景。*

在 LangGraph 里，你是在做图形化架构设计：

彻底解耦的节点：chat_node 只负责聊天并把聊天记录塞进历史；compress_node 只负责做总结并清空历史。它们之间互不关心，甚至可以由两个团队分开写。

声明式的路由控制：通过 add_conditional_edges，你的业务流程变成了清晰的图纸。如果以后老板说：“不要满 3 轮才总结，改成 Token 超过 2000 再总结”，你只需要去改 should_compress_router 这一个控制面函数，数据面的所有节点代码连一个标点符号都不用动。

完美解决死循环：在复杂的 Agent 项目中，通过给边加上流向计数，LangGraph 可以极其简单地限制“大模型调用工具如果连续失败超过 3 次，立刻强制引流到报错退出节点”，这是 LangChain 绝对无法优雅做到的。

## persistent streaming 
持久化记忆机制
流失输出

- thread_id 是持久化硬路由的唯一凭证：
在最后一部分的测试中你会清晰地看到，当我们在 config 中传入 thread_id: "user_session_999" 时，大模型在第二轮能极其精准地叫出你叫“小明”并知道你是“Python工程师”，这是因为 MemorySaver 把整个图的中间状态切片绑在了解析键 user_session_999 下。当换成 user_session_888 后，大模型立刻表示不知道你是谁，完美实现了多用户并行的状态隔离与会话管理。

- stream_mode="updates" 的机制：
在 graph.stream(...) 中，我们使用了 stream_mode="updates"。这是 LangGraph 原生的高层流式抽象。只要图内的任何一个节点（Node）执行完毕并返回了更新数据，这个生成器就会立刻向外扔出（yield）一个字典，其格式为 { "节点名称": { 节点的 return 字典 } }。在复杂的 Multi-Agent 架构中，前端就是靠这个机制来实时感知到底现在是 Planner 在思考，还是 Generator 在抓取网页的。

- add_messages 减负状态控制：
在定义 AgentState 时，对 messages 键使用了 Annotated[..., add_messages]。这意味着我们在节点中不需要手写 state['messages'] + [new_msg] 去痛苦地拼接历史列表，LangGraph 只要看到节点返回了新的 {"messages": [response]}，就会依据底层 reducer 协议，自动增量 append 进该 thread 的全局持久化历史中。