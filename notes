1. model call tool 无限循环不结束问题怎么处理？
强制注入结束逼迫prompt；max loops限定，强制总结；搜索关键词重复度threshold，强制总结
2. 为什么model和model binding tool 要分开？
model一直带有toolmessage会导致DS 继续调用model，不做final summary。
3. 程序内保留有限历史message会什么有时候不能清除老message记忆？
DS server middleware 的context caching 可能导致记忆传递。利用prefix matching技术cache 64 tokens。
如何去除？prompt 开头增加动态时间。让caching失效